# Compute GitOps Helm Chart
This helm chart contains the resources used to create and configure the VPC GitOps instance on Compute clusters. It will create an ArgoCD instance, AppProject, and ClusterRole and ClusterRoleBinding for the VPC GitOps Service Account

# TODO UPDATE THIS - VPC is not needed.